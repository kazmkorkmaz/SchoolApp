@model IEnumerable<Lecturer>


    <h1>Lecturers</h1>

    <p class="text-end">
        <a asp-action="Create" class="btn btn-secondary"><i class="fa-solid fa-plus"></i> Create New</a>
    </p>
    <table class="table table-hover table-bordered table-striped">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.FirstName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.LastName)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var lecturer in Model) {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => lecturer.FirstName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => lecturer.LastName)
                </td>
                <td>
                    <div class="button-group" role="group">
                        <a class="custom-btn edit-btn" asp-action="Edit" asp-route-id="@lecturer.Id">
                            <i class="fas fa-edit"></i> Edit
                        </a>
                        <a class="custom-btn details-btn" asp-action="Details" asp-route-id="@lecturer.Id">
                            <i class="fa fa-info-circle"></i> Details
                        </a>
                        <button data-id="@lecturer.Id" class="custom-btn deleteBtn" type="button">
                            <i class="fa fa-trash"></i> Delete
                        </button>
                    </div>
                </td>
            </tr>
            }
        </tbody>
    </table>